.wordsize 16              ; sets the machine wordsize
.regcnt    2              ; 4 general purpose registers
.maxmem   10              ; max memory size is 32 bytes

start:
MOVZ R0 data

LDUR R1 [R0, #0]
ADDI R1 R1 #1
STUR R1 [R0, #0]

SUBIS ZERO R1 #1000
B.LT start
HALT

data:
.double 0b0